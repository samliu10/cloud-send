import React from 'react';
import NewChannel from './NewChannel';

export default ({ currChannel, changeChannel, addChannel, channels }) => {
    // render new channel button with addChannel callback
    // highlight current channel
    // for each channel, render channel name

    return (
        <div>

        </div>
    )
}