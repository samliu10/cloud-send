import React, { useState } from 'react';

export default ({ addMessage }) => {

    const [newMessage, setNewMessage] = useState("");

    // create a form and use setNewMessage with onChange 
    // button onClick (or form onSubmit) call addMessage callback function


    return (
        <div>

        </div>
    )
}